# 새 채팅 시작 프롬프트 — 2026-05-10

> Phase 1C STEP 19-A booking 통합 완료 → STEP 19-B 와꾸 정비 진입 직전 시점.

---

## 첨부 파일 목록 (Claude Chat 프로젝트 지식에 첨부)

1. **`CHAT_PROJECT_KNOWLEDGE.md`** — 프로젝트 전반 지식 베이스
2. **`TATOA_STATUS_REPORT_20260510.md`** — 5/10 코드 상태 보고서 (이번 세션 핵심 인덱스)
3. **`홈페이지_제작_생성_웹사이트.pdf`** — 와꾸 톤 참고 모델 (시술안내 페이지)

---

## 첫 메시지 템플릿 (Jason 복붙용)

```
안녕. 5/10 보고서 기반으로 이어서 작업하자.

직전 작업: STEP 19-A 시리즈 (booking 컴포넌트 통합) + STEP 18-3 시리즈 (줄바꿈 fix) 완료.

**해소된 항목**:
- Phase 1B STEP 9.2 booking 통일 미완 → 해소
- 18-3 회귀(평문 트림) → 18-3-fix-3에서 보강

**오늘 작업할 것**: STEP 19-B (booking 페이지 와꾸 정비)
- 19-B-1 진단부터 시작 — 시술안내 톤 외곽 와꾸 분석
- 참고 모델: app/preview/site/[branchSlug]/treatments/page.tsx (118줄)
- 적용 위치: PreviewBooking 외곽 또는 wrapper (booking-preview.tsx)

**다음 단계**: 19-B-2/3 → 19-C (가상폭/scale 1280/375)

먼저 19-B-1 read-only 진단 프롬프트부터 작성해줘.
보고서 §"다음 세션 우선순위" 참조.
```

---

## 즉시 실행 시퀀스

### 1. Cowork 코드 스캔 의뢰 (선택)
```
"5/10 보고서가 현재 코드와 일치하는지 검증해줘. STEP 19-A booking 통합 + 18-3-fix-3 적용 상태 확인."
```

### 2. STEP 19-B-1 진단 프롬프트 작성 (Chat → Code)

읽기 전용 진단 — 다음 위치만 view + 분석:
- `app/preview/site/[branchSlug]/treatments/page.tsx` 전체 (118줄)
- `components/site/sections/booking-preview.tsx` 외곽 wrapper 영역 (BookingPhoneScreen, line 1199~1323)
- `app/preview/site/[branchSlug]/booking/page.tsx` (31줄, 현재 외곽 wrapper 없음)

진단 출력:
- treatments 와꾸 톤 핵심 구조 (헤더, max-w, 배경, padding)
- 현 booking에 부재한 외곽 요소 목록
- 19-B-1 적용 위치 후보 (booking-preview wrapper / page.tsx wrapper / 양쪽)
- 의존성 분석 (어드민 미리보기에 와꾸 적용 시 폰 프레임 충돌 여부)

### 3. Jason 시각 검증 → 19-B-2 / 19-B-3 분기

19-B-1 진단 결과 + Jason 시각 검토 후:
- 19-B-2 (카드 톤 정비) 진행 여부
- 19-B-3 (모바일/PC 반응) 진행 여부
- 19-C (가상폭/scale) 합칠지 분리할지

---

## 기억할 컨텍스트 요약 (5/10 시점)

### 직전 세션 학습 사항 (5/9 → 5/10)

1. **별도 파일 + props 주입 패턴 (P14 등재)**
   - mock 의존 컴포넌트는 `SectionPreviewBlock`에 직접 export 시 테스트 사이트에 mock 혼입 위험
   - 별도 파일 신설 + caller가 데이터 정제 후 props 주입
   - 컴포넌트는 받은 데이터만 표시 (필터/매핑 금지)

2. **잠복 필터 제거 학습 (19-A-4-fix)**
   - `Step1Branch` 내부 `branches.filter(b => b.isPublic)` 잠복 → caller가 단일 지점 줘도 0건
   - 책임 분리 위반 → caller가 정제했으면 컴포넌트는 그대로 표시

3. **줄바꿈 보존 인프라 최종 형태 (18-3-fix-3)**
   - `hadHtml` 분기 (HTML/평문 모드 자동 판별)
   - `\n{3,}` → `\n\n` 압축 (3 빈 줄 이상 → 1 빈 줄)
   - HTML일 때만 `^\n` 1개 trim (평문은 첫 빈 줄 보존)

4. **Migration 단방향 정책**
   - `migrateBookingValues`: `pageTitle → bkTitle` 단방향 (옛 키 keep)
   - 자연 소멸 옛 키 7개 (pageSubtitle, ctaHeadline 등) — 매핑 안 함
   - 신규 컴포넌트가 옛 키 직접 참조 0건 grep 검증

### Code 작업 룰 (검토 후 헤더 추가 권장)

```
1. Edit 도구 우선 — Write 도구로 대용량 파일 작성 금지
2. 단계 분리 준수 — Chat이 명시한 sub-step을 임의로 합치지 말 것
3. 모든 수정 후 검증 — wc -l + grep + tsc --noEmit 결과를 응답에 포함
```

### Phase 1B/1C 잔여 작업

| 우선순위 | STEP | 내용 |
|----------|------|------|
| 1 | 19-B-1~3 | booking 와꾸 정비 (시술안내 톤) |
| 2 | 19-C | booking 가상폭/scale 신형 1280/375 |
| 3 | 8.2-A F1+F2 | DirectionsPreviewPage 가상폭/scale (프롬프트 준비됨) |
| 4 | 9.2 cart/popup | cart/popup 컴포넌트 통일 |
| 5 | 6-3, 6-4 | 아이콘 복수화 어드민 UI |

### 검토 항목 (Jason 결정 대기)

1. Code 자체 판단 단계 합치기 룰 추가 시점
2. booking-page.tsx 1,275줄 감소 시점 Write 사용 흔적 재검증
3. 자연 소멸 옛 booking 키 cleanup migration 시점
4. STEP 17 카피라이트 이중 노출 처리

---

## 주요 식별자 빠른 참조 (5/10 신규)

| 식별자 | 위치 |
|--------|------|
| `PreviewBooking` | `components/site/sections/booking-preview.tsx:1332` |
| `BranchInfo` 타입 | `booking-preview.tsx:29` |
| `PreviewBookingProps` 타입 | `booking-preview.tsx:36` |
| `migrateBookingValues` | `lib/branch-website-store.tsx:279` |
| `migrateSnapshot` booking 분기 | `lib/branch-website-store.tsx:311-317` |
| 어드민 caller (3곳) | `booking-page.tsx:392, 418, 430` |
| 테스트 사이트 caller | `app/preview/site/[branchSlug]/booking/page.tsx:24` |
| `singleBranches` 계산 로직 | `booking-page.tsx` 내부 |

---

## 보고서 외 참고 자료

- 5/9 보고서 (`TATOA_STATUS_REPORT_20260509.md`) — 푸터 시스템 + 줄바꿈 인프라 상세
- 5/8(파일명 5/5) 보고서 (`TATOA_STATUS_REPORT_20260505.md`) — Phase 1C 초기 상태
- 새 채팅에서 두 보고서 모두 첨부 가능 (참조 우선순위: 5/10 > 5/9)
